import java.util.*;
public class coins 
{
    public static void main(String args[])
    {
        Scanner obj = new Scanner(System.in);
        int num_of_coins = obj.nextInt();
        int coins[] = new int[num_of_coin];
        int  target = obj.nextInt();
        for(int i=0;i<coins.length;i++)
        {
          num_of_coins[i] = obj.nextInt();
        }

        subset(num_of_coins,)
    }
}
